package player;

public class HitPoints
{
    int hitPoints;    // global variable
    int constitution; // from player ability

    public void hitPoints()
    {
        if (hitPoints == 0)
        {
            //to be used in future builds
            //d10 d = new d10();            
            //hitPoints = d.d(10) + constitution;
        }
    }
}
